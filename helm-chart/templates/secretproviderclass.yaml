apiVersion: secrets-store.csi.x-k8s.io/v1alpha1
kind: SecretProviderClass
metadata:
  name: prova-aws-secrets-db
spec:
  provider: aws
  parameters:
    objects: |
        - objectName: "pagopa-dev-atm-layer/rds/credentials"
          objectType: "secretsmanager"
          jmesPath: 
              - path: username
                objectAlias: username
              - path: password
                objectAlias: password
        - objectName: "prova/secret"
          objectType: "secretsmanager"
          jmesPath: 
              - path: test
                objectAlias: test
              - path: provo
                objectAlias: provo
  secretObjects:
    - secretName: prova-aws-secrets-db  # the k8s secret name
      type: Opaque
      data:
        - objectName: "test"  # reference the corresponding parameter
          key: DB_USERNAME
        - objectName: "provo"  # reference the corresponding parameter
          key: DB_PASSWORD
    - secretName: prova-aws-secrets-webuser
      type: Opaque
      data:
        - objectName: "test"  # reference the corresponding parameter
          key: WEB_USER
        - objectName: "provo"  # reference the corresponding parameter
          key: WEB_PASSWORD
